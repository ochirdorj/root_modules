name: ami_builder
on:
  push:
    branches:
      - main
    paths:
      - 'ami_builder/**'
      - '.github/workflows/ami_builder.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'ami_builder/**'
      - '.github/workflows/ami_builder.yml'
  workflow_dispatch:

concurrency:
  group: ami_builder
  cancel-in-progress: true

permissions:       
  contents: read
  pull-requests: write
  id-token: write

jobs:
  terraform:
    uses: ./.github/workflows/terraform.yml
    with:
      working_directory: ami_builder
      tf_version: '1.13.0'
      skip_check: 'CKV2_AWS_62,CKV_AWS_145,CKV_AWS_144,CKV_AWS_19'
    secrets: inherit